#include <numeric>
#include <vector>
#include <algorithm>
#include <numeric>
using namespace std;

// class Solution {
// public:
//     string getPermutation(int n, int k) {
//         string res = "";
//         vector<int> nums;
//         for (int i = 1; i <= n; i++)
//             nums.push_back(i);
//         long prod = accumulate(nums.begin(), nums.end(), 1, multiplies<int>());
//         long a, k = prod;
        
//     }
// };

class A {
public:
    int a;
    double b;
private:
    DISA
};

int main(int argc, char const *argv[]) {
    return 0;
}